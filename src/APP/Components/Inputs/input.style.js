// const className = " border-2  rounded-lg py-2 px-4 w-full";

const base = "border-2 rounded-lg py-2 px-4 mb-8";

const inputClass = {
  error: `${base} border-red-600`,
  base: base,
};

export default inputClass;
